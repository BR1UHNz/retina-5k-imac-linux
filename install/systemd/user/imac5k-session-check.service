[Unit]
Description=Native 5K: confirm the stitched panel is active; show pending driver notices
After=graphical-session.target
PartOf=graphical-session.target

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/imac5k-session-check

[Install]
WantedBy=graphical-session.target
